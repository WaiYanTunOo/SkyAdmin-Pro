{
  "name": "skyadmin-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:init": "node -e \"console.error('DEPRECATED: use npm run db:migrate (wrangler d1 migrations apply). schema.sql is reference-only.'); process.exit(1)\"",
    "db:init:local": "node -e \"console.error('DEPRECATED: use npm run db:migrate:local. schema.sql is reference-only.'); process.exit(1)\"",
    "db:migrate": "wrangler d1 migrations apply skyadmin-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply skyadmin-db --local",
    "db:migrate:new": "echo \"Create migrations/NNNN_name.sql then run db:migrate\""
  },
  "dependencies": {
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.127.1"
  }
}
