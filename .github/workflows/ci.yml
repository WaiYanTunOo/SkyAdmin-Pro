name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Lint with ruff
        run: ruff check .

      - name: Check formatting with ruff
        run: ruff format --check .

      - name: Run tests
        run: pytest tests/ -v --tb=short --ignore=tests/test_performance_clients.py --ignore=tests/test_performance_stress.py
        env:
          SKYADMIN_SKIP_DAILY_CHECK: "1"
          SKYADMIN_PROCESS_OFFLOAD: "0"

      - name: Audit Python dependencies
        run: pip-audit --strict

      - name: Security scan with bandit
        run: bandit -r skyadmin_pro/ -c pyproject.toml --severity-level medium

  perf:
    # Dedicated performance gate: budget tests block the build, and per-test
    # timings upload as JUnit XML for run-over-run history (90-day retention).
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Run performance budgets
        run: pytest tests/test_performance_clients.py tests/test_performance_stress.py -q --tb=short --durations=15 --junitxml=perf-results.xml
        env:
          SKYADMIN_SKIP_DAILY_CHECK: "1"

      - name: Upload perf timings
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: perf-timings-${{ github.sha }}
          path: perf-results.xml
          retention-days: 90

  qt-shell:
    # Phase 4: Qt6 shell parity gate. Offscreen platform (no display on the
    # runner); PySide6 comes from requirements-qt6.txt, kept out of the
    # default install so the CustomTkinter build stays lean.
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt -r requirements-qt6.txt
          sudo apt-get update -qq
          sudo apt-get install -y -qq libegl1 libxkbcommon0 libdbus-1-3 > /dev/null

      - name: Run Qt shell tests (offscreen)
        run: pytest tests/test_qt_shell.py tests/test_qt_database_tasks.py tests/test_qt_document_hub.py tests/test_qt_office_hub.py tests/test_qt_settings.py tests/test_qt_utilities.py tests/test_qt_walkthrough_parity.py -q --tb=short
        env:
          QT_QPA_PLATFORM: offscreen
          SKYADMIN_SKIP_DAILY_CHECK: "1"
          SKYADMIN_CIPHER_SALT: "ci-test-cipher-salt"

  worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skyadmin-worker

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: skyadmin-worker/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck worker
        run: npx tsc --noEmit

      - name: Test worker
        run: npm test

      - name: Security scan with semgrep
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/typescript
          generatingCode: false

      - name: Audit Worker dependencies
        run: npm audit --audit-level=moderate
